# 開發日誌：2026-08-01 首頁歷史戰報數量限制優化

## 📝 修改摘要

本工作階段完成了首頁歷史戰報數量的顯示限制優化：
- **首頁歷史戰報數量限制**：將首頁往下拉展示的「歷史戰報存檔」限制為僅顯示最新 6 場戰報卡片，避免因戰報累積導致頁面過長與佔用空間。

---

## 🛠️ 詳細改動內容

### 一、 首頁 HTML 靜態結構裁切
- **[index.html](file:///c:/GitHub/MLB-daily/index.html)**：
  - 裁切 `<!-- REPORTS_LIST_START -->` 與 `<!-- REPORTS_LIST_END -->` 之間的歷史戰報卡片數量，僅保留最新 6 場戰報（2026-07-31 至 2026-07-26），移除較舊的歷史戰報卡片。

### 二、 Python 自動化更新邏輯設限
- **[fetch_mlb.py](file:///c:/GitHub/MLB-daily/fetch_mlb.py)**：
  - 在 `update_index_html()` 函式中，針對歷史戰報日期列表取前 6 筆（`for date_str in sorted_dates[:6]:`），確保未來每日自動擷取並更新首頁時，歷史戰報分頁卡片恆定維持最多顯示最新 6 場。

### 三、 專案進度文件更新
- **[docs/progress.md](file:///c:/GitHub/MLB-daily/docs/progress.md)**：
  - 新增「階段十四：首頁歷史戰報數量限制優化」並標記為完成。

---

## 📦 Git Commit Message

```text
feat: 限制首頁歷史戰報展示數量為最新 6 場

- 調整 index.html 歷史戰報存檔清單，僅保留最新 6 場戰報卡片
- 修改 fetch_mlb.py update_index_html() 邏輯，設限僅生成與渲染 sorted_dates[:6] 的戰報卡片
- 更新 docs/progress.md 新增階段十四說明
```
