# 開發日誌：2026-07-21 (優化卡片版面與歷史存檔目錄分層)

## 本次開發摘要
完成階段六的所有優化與長期維護任務。包括：重構戰報卡片版面為「方案 B」上下滿版與雙欄佈局；引入 MLB 官方 `team-cap-on-dark` SVG 隊徽與 CSS `filter: drop-shadow` 白色外發光描邊效果；將產出戰報儲存路徑重構為年份與月份分層子目錄（`reports/YYYY/MM/`），並調整首頁路徑連結更新與 Discord 連結解析；實作「昨日無賽事自動跳過」空頁防錯機制，確保系統在休賽季與休兵日穩定運行。

---

## 新增與改動項目

### 1. 戰報卡片版面重構為「方案 B」
- **修改** [report_template.html](file:///c:/GitHub/MLB-daily/templates/report_template.html) & [fetch_mlb.py](file:///c:/GitHub/MLB-daily/fetch_mlb.py)：
  - **結構重構**：將卡片分割為 `.game-card-upper` (滿版對戰比分) 與 `.game-card-lower` (下半部雙欄細節)。
  - **雙欄自適應**：下半部左欄為「投球決定」，右欄為「單場焦點球員」。大螢幕上並排展示，行動裝置自動折疊成單欄。
  - **外觀細節**：加入了虛線分割線（`border-top: 1px dashed var(--card-border)`）區隔上下層，調整各區塊內距，提升排版緊湊度。

### 2. 引入官方 SVG 隊徽與外發光效果
- **修改** [report_template.html](file:///c:/GitHub/MLB-daily/templates/report_template.html) & [fetch_mlb.py](file:///c:/GitHub/MLB-daily/fetch_mlb.py)：
  - **隊徽替換**：將原先文字 `.team-badge` 換成 `<img>` 標籤，讀取官方 SVG：`https://www.mlbstatic.com/team-logos/team-cap-on-dark/{team_id}.svg`。
  - **外發光濾鏡**：在 CSS 中為 `.team-logo` 套用 `filter: drop-shadow(0 0 4px rgba(255, 255, 255, 0.25))`，解決黑底背景下暗色系隊徽對比度不足的問題。

### 3. 實作歷史戰報目錄分層（`reports/YYYY/MM/`）
- **修改** [fetch_mlb.py](file:///c:/GitHub/MLB-daily/fetch_mlb.py)：
  - **輸出目錄優化**：戰報輸出路徑由原先平鋪的 `reports/{date}.html` 改為分層子目錄，即 `reports/YYYY/MM/YYYY-MM-DD.html`。
  - **首頁連結與 Discord 解析更新**：修改 `update_index_page` 及 `send_discord_notification`，使最新戰報與歷史戰報清單的連結路徑能正確解析出子資料夾。

### 4. 實作無賽事日期防錯與自動跳過
- **修改** [fetch_mlb.py](file:///c:/GitHub/MLB-daily/fetch_mlb.py)：
  - **休賽期適應**：檢測到昨日賽事數量為 0 時，腳本不再嘗試處理空數據，而是直接生成「今日無賽事」報告並寫入 metadata 與 index 頁，防止排程報錯中斷。

---

## 驗證狀態
- [x] 重構 `templates/report_template.html` 卡片版面為「方案 B」
- [x] 在網頁上引入 MLB 官方 `team-cap-on-dark` SVG 隊徽與 CSS `filter: drop-shadow` 外發光描邊效果
- [x] 實作歷史戰報目錄分層（`reports/YYYY/MM/`）與 `index.html` 連結解析對接
- [x] 實作「昨日無賽事自動跳過」防錯機制
- [x] 本地執行測試 `python fetch_mlb.py --date 2026-07-20` 與 `2024-09-15`（通過，成功在 subfolder 產生檔案且首頁連結更新完全正確）
- [x] 離季無賽事測試 `python fetch_mlb.py --date 2024-12-25`（通過，自動跳過並生成今日無賽事頁面）
- [x] 更新進度表與開發文件
